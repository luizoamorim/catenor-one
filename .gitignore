# dependencies and build output
node_modules/
dist/
coverage/
*.tsbuildinfo

# local secrets — never committed (names only in .env.example)
.env
.env.*
!.env.example

# local spikes/experiments (also ignored by scratch/.gitignore)
scratch/

# OS / editor
.DS_Store

# Prisma generated client (apps/api, prisma generate)
apps/api/src/infrastructure/persistence/prisma/generated/

# Demo run records are regenerated per run (commit only curated ones under artifacts/)
artifacts/demo/

# Clean-room demo state (public refs, runtime keys, private evidence) — local only, never committed
.catenor-demo/
