# S001 — Trust Anchor Admission
## Tasks

**Status:** Rev 2.6 — Rev 2.1–2.5 approved 2026-09-10. Rev 2.6 clarifies key terminology (T0.4, PLAN §13.0) and keeps the base64 sealed-context transport as APPROVED DESIGN, NOT YET SIMULATION-CONFIRMED (T8.2) — **APPROVED** (final Phase 0 documentation review, 2026-09-10). T0.9 complete. No implementation task is started.
**Rule:** a task is checked only when its code **and** listed tests exist and pass (CLAUDE.md). Live/manual tasks are checked only when the named artifact exists.
**Hackathon Delivery Mode (2026-09-11, until the ETHOnline submission):** execution follows `docs/hackathon/plans/2026-09-11-006-ethonline-delivery-fast-lane.md` — the smallest credible end-to-end demo path first, no longer phase by phase. Requirements, facts, policy, evidence profiles and security claims are unchanged. A task may be implemented only as far as the demo needs; it stays unchecked until its full listed scope and tests exist. `[POST-DEMO]` marks work deliberately deferred until after the submission (kept for traceability, never deleted).

Format: **ID — title** · `deps` · `AC` · `TV` · deliverable · done-when.

```text
[FAST]       on the P0 Fast Lane (PLAN §0) — execution order
[P1] / [P2]  never blocks a [FAST] task
[HUMAN]      maintainer action
[SPIKE]      throwaway investigation under scratch/ (git-ignored); result recorded in PLAN/BUILD_LOG; no production code
[STOP-GATE]  failure ⇒ STOP and report to the maintainer; no silent substitute
```

---

## Phase 0 — Human gates, subagent, spikes

- [x] **T0.1 [HUMAN] — Approve PLAN/TASKS Rev 2** · deps: — · approved by the maintainer on 2026-09-10 with final decisions Q1 (refined: `verificationMethodCommitment`), Q2, Q3 and the commitment-wording correction (PLAN §33.3). BUILD_LOG entry still to be written (T18.5).
- [ ] **T0.2 [HUMAN][FAST] — CRE access** · deps: — · AC-029, 030 · `cre login`; `cre whoami` shows deploy access; **Confidential Workflows private-beta enrollment**; **private registry** available · done-when: confirmed in writing (blocker B1). Mainnet registry only with explicit maintainer approval. · **Status 2026-09-10:** deploy access ✓ and private registry ✓ (per maintainer); Confidential Workflows enrollment submitted — in Chainlink's queue ("todo", ≈24 h typical). Open until enrollment is confirmed.
- [ ] **T0.3 [HUMAN][FAST] — Accounts** · deps: — · Sumsub **sandbox** (KYC level ✓ `id-only`, token ✓ used in T0.8; **KYB 2.0 company level ✗ — tenant entitlement missing, B11**), Privy app (email login, identity tokens on), Railway project (staging + production, Postgres) · done-when: accounts exist; no secret value in repo/chat.
- [ ] **T0.4 [HUMAN][FAST] — Privy signing-wallet and authorization-key provisioning (+ other runtime secrets)** · deps: T0.3 · per PLAN §13.0 (D34, D36): **2 Catenor Ed25519 signing keys** — (A) Credential Assertion Key = dedicated Privy Solana wallet associated with the candidate Organization (Catenor One managed signing infrastructure; reference-implementation choice, not a protocol requirement), (B) Bootstrap Endorsement Key = completely separate Privy Solana wallet controlled by the Bootstrap Authority (the maintainer) — each wallet created by T5.2 / T5.3; **4 Privy P-256 authorization (control) keys**, generated here: assertion management-owner authorization key and bootstrap management-owner authorization key (maintainer custody, never in the runtime), assertion runtime-signer authorization key (Railway sealed `CATENOR_ASSERTION_RUNTIME_AUTHORIZATION_KEY`) and bootstrap runtime-signer authorization key (Railway sealed `CATENOR_BOOTSTRAP_RUNTIME_AUTHORIZATION_KEY`); the P-256 keys are never called the Credential Assertion Key or Bootstrap Endorsement Key, and no key is reused across the two wallets; CRE trigger EVM key (unfunded, trigger-only), `CATENOR_INTERNAL_API_TOKEN` (32 random bytes), `OPERATOR_REF_KEY` · done-when: public parts recorded; private parts only in Railway sealed vars / Vault DON — except the two management-owner authorization keys, which stay in maintainer custody (never in Railway, the repo or the runtime).
- [x] **T0.5 [SPIKE][STOP-GATE][FAST] — Privy Ed25519 signing semantics** · deps: T0.3 · AC-011–013 · TV-D01, I06 · **PASSED WITH APPROVED DESIGN AMENDMENT (2026-09-10)**: dedicated Solana wallet provisioned; public key from the address (no export); raw-byte `signMessage` verifies as plain Ed25519 with `@noble/curves`; owner authorization enforced; `signTransaction`, `exportPrivateKey`, `exportSeedPhrase` denied; `raw_sign` unsupported for Solana; Privy `byte_length` fails for binary messages → Catenor signer boundary (D33); owner/signer separation (D34); send-denial UNCONFIRMED LIVE, documentation-supported only (D35). T0.5 proves the Ed25519 primitive, **not** `eddsa-jcs-2022` interoperability · evidence: `spikes/T0.5-privy-assertion-key.md`.
- [ ] **T0.5q [HUMAN][NON-BLOCKING] — Privy question (Q6)** · deps: — · ask Privy whether policy `byte_length` intentionally operates on the decoded/textual message for Solana `signMessage`, and whether a raw-byte length predicate exists · done-when: answer recorded in the T0.5 findings. Nothing waits on it.
- [x] **T0.6 [FAST] — `cre-engineer` project subagent** · deps: T0.1 · deliverable: `.claude/agents/cre-engineer.md` per PLAN §17.7 · **COMPLETE (2026-09-10)**: reviewed and approved by the maintainer after three corrections (read-only account/session inspection, login/logout only when authorized; local `.env` handling rule; simulation never implies deployment); recognized by Claude Code; `chainlink-cre-skill` preloaded via `skills:` frontmatter (B9 resolved).
- [x] **T0.7 [SPIKE][STOP-GATE][FAST] — CRE Confidential runtime (via cre-engineer)** · deps: T0.2 (login), T0.6 · AC-023, 024 · **COMPLETE — findings approved 2026-09-10 (PLAN D37); all runtime results are SIMULATION-CONFIRMED only**: official scaffold (B8 resolved); `handlerInTee`/`TeeRuntime`; batched `getSecrets` (3 names); noble HMAC/HKDF/SHA-256 + JCS; AES-256-GCM open of a Node-sealed context with 5/5 tamper cases failing closed (B3 simulation-confirmed); `HTTPClient` inside `handlerInTee`; `runtime.now()`; production-like limits file enforces 5 calls and 10,000/100,000-byte limits (B6 simulation-confirmed). Not confirmed: deployed-TEE behavior (B1); base64 sealed-context transport = APPROVED DESIGN, NOT YET SIMULATION-CONFIRMED (T8.2 tests it in the CRE QuickJS/WASM runtime; T16.2 re-checks the whole sealed path deployed); timeout values > 10 s · evidence: `spikes/T0.7-cre-runtime.md`.
- [x] **T0.8 [SPIKE][FAST] — Sumsub sandbox semantics (individual / representative)** · deps: T0.3 · AC-019 (representative leg) · TV-L03 · **PASSED and approved 2026-09-10 (D25)**: real Sumsub sandbox API, synthetic applicants; auth; level list (`id-only`); bindingRef ↔ `externalUserId` verified (echo + lookup; charset `[a-z0-9-]`); `testCompleted` GREEN verified; RED/`SANCTIONS`/`FINAL` verified; field shapes and sizes recorded; normalization decisions D26–D28 recorded in PLAN §20.8 · evidence: `spikes/T0.8-sumsub-sandbox.md` §6 (sanitized).
- [ ] **T0.8b [SPIKE][BLOCKED — B11] — Sumsub company / KYB semantics** · deps: KYB entitlement for the sandbox tenant [HUMAN] · company applicant (KYB 2.0), representative linking (`fixedInfo` vs `info` beneficiaries, `memberOf`), company checks / registry status, company `testCompleted`, role-verification strength · done-when: company-side rules of PLAN §20.4 and Bootstrap Configuration company values filled from observations. **Do not attempt to bypass or fake the entitlement**; until then the MOCK fixture (T8.3a) is used.
- [x] **T0.9 [HUMAN][FAST] — Source-of-truth cleanup** · deps: T0.1 · apply (or amend) PLAN Appendix E.1 to SPEC.md, ACCEPTANCE.md, TEST-VECTORS.md in a separate intentional commit · done-when: no LLM requirement remains in S001 source of truth; secret inventory = 3; COMMITMENT_ONLY reflected; this gates T1.1. · **COMPLETE (2026-09-10):** maintainer-approved; separate intentional source-of-truth commits `f6b571e` (Rev 2 cleanup) and `2772744` (alignment through Rev 2.6). No LLM requirement remains; secret inventory = 3; COMMITMENT_ONLY reflected; T1.1 is unblocked by T0.9.

## Phase 1 — Foundation

- [x] **T1.1 [FAST] — Monorepo scaffold** · deps: T0.1, T0.9 · root `package.json`, `pnpm-workspace.yaml`, `tsconfig.base.json`, `.nvmrc`, ESLint/Prettier, empty package entrypoints · done-when: `pnpm -r build` + `pnpm -r lint` pass.
- [x] **T1.2 [FAST] — Test harness** · deps: T1.1 · Vitest workspace; `test-vectors/s001/` + loader · done-when: sample test reads a golden vector.
- [x] **T1.3 [FAST] — Boundary enforcement** · deps: T1.1 · `.dependency-cruiser.cjs` (PLAN §2.3) · done-when: CI fails on a deliberate violation.
- [ ] **T1.4 [FAST] — CI** · deps: T1.1–T1.3 · lint, typecheck, unit tests, dependency-cruiser, secret-scan stub · done-when: green. · **Status 2026-09-10:** workflow `.github/workflows/ci.yml` added; the identical command sequence (`pnpm check`) is green locally; stays unchecked until the first GitHub Actions run is green (nothing pushed yet).
- [x] **T1.5 [FAST] — Provenance for tooling** · deps: T1.1 · versions/licenses in PROVENANCE.md; BUILD_LOG entry · AC-073.

## Phase 2 — Domain packages (framework-free)

- [x] **T2.1 [FAST] — `packages/audit`** · deps: T1.2 · RFC 8785 JCS, `commit()`, `Commitment`, audit event model, `chainEvent()` · tests: JCS vectors, commitment goldens, chain tamper detection · AC-039, 060. · **Status 2026-09-11:** [REF-IMPL] chain encoding binds the Trust Domain — `eventHash = SHA-256(JCS(event ∪ {trustDomain}) ‖ prevHash raw 32 bytes)`; reclassifying an event into another Trust Domain changes its hash (maintainer review of T3.1).
- [x] **T2.2 [FAST] — `packages/identity`** · deps: T1.2 · `CatenorDid` (from 16 random bytes only), `SubjectType`, `Subject`, `VerificationMethodId`, `Multikey`, `VerificationMethod`, `DidDocument`, `KeyPurpose`, `KeyManagementReference` · tests: TV-C01, C02, C03; Multikey round-trip; assertion/financial purpose collision rejected · AC-007–013.
- [x] **T2.3 [FAST] — `packages/credentials` eddsa-jcs-2022** · deps: T2.1, T2.2, T0.5 · `DataIntegrityProof`, `hashData()` (64 bytes — current profile value), `verify()` · tests: W3C vc-di-eddsa vectors where published + own goldens; tampering → false. This task (with T5.2's end-to-end check) is what establishes `eddsa-jcs-2022` interoperability; T0.5 proved only the Ed25519 primitive (D3).
- [x] **T2.4 [FAST] — `packages/policy` document + hash** · deps: T2.1 · `trust-anchor-admission.v1.json`; schema check vs protocol `policy.schema.json`; `policyHash()` · tests: golden hash; faithful-to-SPEC test · AC-039.
- [x] **T2.5 [FAST] — `packages/policy` evaluator + Decision** · deps: T2.4 · `FactName`, `VerifiedFact`, `FactSet` (drops unknown), `evaluate()` (missing → DENY, trace MISSING ≠ FALSE), `Decision` validated against protocol `decision.schema.json` · tests: TV-F01–F11 (F12 retired), ERROR on integrity failure · AC-035–038, 040, 043.
- [x] **T2.6 [FAST] — `packages/authority` bootstrap / endorsement / aggregate / record** · deps: T2.3, T2.5 · `TrustDomainId`, `BootstrapConfiguration` + hash, `BootstrapEndorsement` (incl. `verificationMethodCommitment` = SHA-256(JCS(canonical VM {id, controller, type, publicKeyMultibase}))), `TrustAnchorAdmission` state machine (PLAN §4.3), `TrustAnchorAdmissionRecord` (D14 fields), `TrustAnchorStatus` · tests: TV-B03, G01–G06, H03; illegal transitions · AC-005, 006, 044–046, 050, 051, 076.
- [x] **T2.7 [FAST] — `packages/authority` key possession** · deps: T2.3, T2.2 · challenge + verifier (PLAN §14.3 steps 1–8) · tests: TV-D01–D06 with fixed clock + noble keys · AC-014–018. · **Status 2026-09-11 (maintainer decision, option B):** a failure that stops verification before key purpose is evaluated (unknown / consumed / expired challenge, other DID, other context, verification method absent) yields `ASSERTION_KEY_POSSESSION_VALID = false` and no `ASSERTION_KEY_PURPOSE_VALID` fact (trace MISSING); PURPOSE is `false` only when evaluated and invalid; infrastructure failures produce no fact. Policy semantics unchanged.
- [x] **T2.8 [FAST] — `packages/authority` TrustAnchorVerifier** · deps: T2.6 · 12 checks with `basis` CRYPTOGRAPHIC / STRUCTURAL / OPERATIONAL (PLAN §26) · tests: TV-J01–J07; tampered-field matrix incl. VM key replacement under the same ID · AC-063–065, 076.
- [ ] **T2.9 [FAST] — Golden vectors** · deps: T2.1–T2.8 · `test-vectors/s001/*.json` (policy hash, DID doc, challenge + proof, endorsement, record, fact sets, commitment inputs) regenerated by script; consumed by api + workflow tests. · **Status 2026-09-10:** `scripts/vectors/generate-s001.mjs` writes `policy-v1`, `identity` (incl. the TEST-VECTORS §6 `VERIFICATION_METHOD_COMMITMENT`), `key-possession`, `fact-sets` (F01–F11) and `admission` goldens, checked by `packages/authority/src/goldens.test.ts`; signing keys are ephemeral per generation (no private key stored). Open: the evidence-commitment preimage golden, which depends on the T8.5 salt derivation — unchecked until then. Resolved 2026-09-11 (maintainer decision): goldens are signed with TEST-ONLY, NON-SECRET Ed25519 keys derived from public labels (`seed = SHA-256(UTF-8(label))`, `@catenor-one/test-vectors/test-keys`, fenced by dependency-cruiser); regeneration is byte-identical (`pnpm vectors:s001:check`, not yet in CI). Still unchecked until the T8.5 evidence-commitment golden exists.

## Phase 3 — Persistence

- [x] **T3.1 [FAST] — Prisma schema** · deps: T2.6 · models PLAN §10.1 (incl. `ProviderBinding`; no evidence-object model); multiSchema vs prefixes decided from installed Prisma docs; **maintainer schema review** · AC-012, 053–055. · **Status 2026-09-11:** proposal `apps/api/prisma/schema.prisma` drafted; native `schemas` (`catenor_public` / `catenor_private`) validated without a preview flag on Prisma 7.10.0; awaiting maintainer schema review — no migration. **Revision 2 (2026-09-11):** review decisions applied (security wording, Trust-Domain-bound audit chain, initial-root composite FK, signer-mapping invariant, no DB-level `provider = 'sumsub'`); CHECK draft (37 constraints) prepared for T3.2; awaiting final schema review. **COMPLETE (2026-09-11):** final review approved — option-B key purpose kept; C1–C5 CHECKs plus DecisionTrace provenance and challenge `operation = 'ADMIT_TRUST_ANCHOR'`; field-level immutability triggers (initial root set once; `signerRef`; `publicKeyMultibase`); explicit `onDelete: Restrict` / `onUpdate: Restrict` on all 14 relations; `VerificationMethodProjection.createdAt` (projection metadata only); `ProviderBinding` stays provider-generic.
- [x] **T3.2 [FAST] — Initial migration** · deps: T3.1 + approval · unique `initialTrustAnchorDid` · done-when: `prisma migrate deploy` on Testcontainers Postgres. · **COMPLETE (2026-09-11):** `apps/api/prisma/migrations/20260911034806_init` (Prisma-generated DDL + reviewed raw SQL: 39 CHECKs, 3 immutability triggers); Prisma / `@prisma/client` / `@prisma/adapter-pg` 7.10.0; `apps/api/prisma.config.ts`; `pnpm test:integration` deploys it with the real `prisma migrate deploy` on `postgres:17.11-alpine` (Testcontainers) and tests every CHECK, trigger, referential action, no-drift and the single-root / audit-fork / challenge-consume / run-attempt races — 78 tests green locally; CI step added (first GitHub run pending push, see T1.4). Railway not touched.
- [x] **T3.3 [FAST] — Repositories** · deps: T3.2 · `SubjectRegistry` (incl. bindings), `DidStateRegistry`, `AdmissionRepository` (conditional challenge consume), `TrustAnchorRegistry`, `AuditLog` (app-computed hash chain), `UnitOfWork` · integration tests incl. concurrency (TV-D04, K01). · **COMPLETE (2026-09-11, commit e6df1c0):** ports in `apps/api/src/modules/trust-anchor-admission/application/persistence.ports.ts`, Prisma adapters in `apps/api/src/infrastructure/persistence/prisma/prisma-persistence.ts` — **minimum method set for the demo path** (Hackathon Delivery Mode); methods are added only when the vertical path needs them. Tests (Testcontainers): demo path in one UnitOfWork, atomic rollback, concurrent single root (K01), concurrent single-use challenge (D04), one result per run, linear audit chain under 12 concurrent appends (per-Trust-Domain advisory lock), DENY kept private.
- [ ] **T3.4 [FAST] — Public read model** · `[POST-DEMO beyond the reads the demo/judge path uses]` · deps: T3.3 · public-only reads + explicit DTO allowlists · AC-054.
- [x] **T3.5 [P1] — Audit append-only DB trigger** · deps: T3.2 · raw SQL trigger rejecting UPDATE/DELETE + test (D16). · **COMPLETE (2026-09-11, commit 6dd8278):** migration `20260911041606_audit_event_append_only` — row trigger rejects UPDATE/DELETE, statement trigger rejects TRUNCATE; INSERT allowed; tested on Testcontainers PostgreSQL.

## Phase 4 — API skeleton, access gate, provider bindings

- [ ] **T4.1 [FAST] — NestJS app + module** · deps: T1.1, T3.3 · module, zod pipe, problem-details, `/v1/health`, redacting logger · AC-025, 062.
- [ ] **T4.2 [FAST] — Privy operator identity adapter** · deps: T4.1, T0.3 · access-token verify + identity-token verified email; `operatorRef` HMAC · integration test vs Privy dev app · AC-001.
- [ ] **T4.3 [FAST] — Allowlist gate (U1)** · deps: T4.2 · env parser; guard; `BOOTSTRAP_ACCESS_DENIED` audit without email · application tests TV-A01–A03 (nothing created on denial) · AC-001–003.
- [ ] **T4.4 [FAST] — U2 StartInitialAdmission** · deps: T4.3 · Subject + DID + **bindingRefs {COMPANY, REPRESENTATIVE}** (random, prefix/charset per T0.8); returns `providerSetup` to the operator only; **no applicant IDs accepted**; `409 INITIAL_TRUST_ANCHOR_EXISTS` · tests TV-C01, C02; bindingRefs absent from public/judge routes · AC-007, 008. · **Status 2026-09-11:** application use case implemented in `trust-anchor-admission.service.ts` (commit 056d5fd) and exercised end to end on Testcontainers PostgreSQL with the labeled FAKE adapters; remaining: operator gate (T4.3), API route, public/judge-route absence of bindingRefs.
- [ ] **T4.5 [FAST] — U3 AttachProviderReferences** · deps: T4.4 · `PUT …/provider-references`; representative = real Sumsub sandbox applicant ID; company = real applicant ID only when `evidenceSources.company = REAL_SUMSUB_SANDBOX`, otherwise only the MOCK company reference `mock:company-fixture:<scenario>` is accepted (PLAN §4.2, §20.7); private storage `ATTACHED_UNVERIFIED`; re-attach allowed until a run succeeds; audit `PROVIDER_REFERENCES_ATTACHED` (no IDs in details) · application tests incl. U7 refusing to run without attached refs · AC-020, 055. · **Status 2026-09-11:** application use case implemented in `trust-anchor-admission.service.ts` (commit 056d5fd) and exercised end to end on Testcontainers PostgreSQL with the labeled FAKE adapters (MOCK company reference enforced under SYNTHETIC_MOCK); remaining: API route.

## Phase 5 — Key management and DID state

- [x] **T5.1 [FAST] — Signer ports + noble fakes** · deps: T4.1 · `AssertionSigner.signAssertion(signerRef, AssertionSigningInput)` and `BootstrapEndorsementSigner.signEndorsement(EndorsementSigningInput)` take **structured** inputs; no arbitrary-bytes signing method on any port (PLAN §5, §13.4, D33). · **COMPLETE (2026-09-11, commit 056d5fd):** `AssertionSigner.signKeyPossessionProof` / `BootstrapEndorsementSigner.signEndorsement` take structured input only (no arbitrary-bytes method); FAKE noble signers (labeled `fake`) run the same signer boundary.
- [x] **T5.2 [FAST] — Privy Ed25519 assertion signer adapter** · deps: T5.1, T0.5 (passed), T0.4 · Credential Assertion Key wallet created by an admin script with the **assertion management-owner authorization key as owner** and the **assertion runtime-signer authorization key as additional signer** (`override_policy_ids: [P_ASSERT]`); P_ASSERT = ALLOW `signMessage` (no `byte_length`), DENY `exportPrivateKey`/`exportSeedPhrase`, default-deny rest; Multikey from the address; **signer boundary** (PLAN §13.4): build canonical message from structured input → validate format + exact length (current profile 64 bytes) → `signMessage` (bytes, authorized with the runtime-signer authorization key) → verify with `@noble/curves` · integration (Privy dev app): create → sign → noble verify; **end-to-end `eddsa-jcs-2022` proof built by T2.3 and signed via Privy verifies with the T2.3 verifier**; `signTransaction` denied (artifact); **the runtime-signer authorization key cannot** change policy, add/remove signers, change owner, or export (each attempt denied — D34); signer rejects malformed/wrong-length input before calling Privy · AC-011–013 · TV-D01, I06. · **Status 2026-09-11 (commit d37a6d3):** adapter + signer boundary + unit tests + opt-in live test (`pnpm test:privy-live`) + maintainer provisioning script `apps/api/scripts/privy/provision-s001-signers.mjs`; the assertion wallet is created at runtime from public values only. Unchecked until the maintainer runs T0.4 provisioning and the live test passes. **COMPLETE (2026-09-11, commits d37a6d3, 399b606):** LIVE on the Privy development app (`pnpm test:privy-live`, maintainer-provisioned T0.4 values): wallet from public values; Privy-signed `eddsa-jcs-2022` key-possession proof verifies with `verifyKeyPossession` (TV-D01, end-to-end Data Integrity through Privy); runtime-signer key denied export, policy removal/update, owner change and signer removal (HTTP 401); `signTransaction` denial from the T0.5 artifact. Evidence: `artifacts/privy/s001/live-signers.md`.
- [x] **T5.3 [FAST] — Bootstrap signer + setup script** · deps: T5.2, T0.4 · **separate** Bootstrap Endorsement Key wallet (Solana) created by `scripts/bootstrap/create-bootstrap-signer.ts` run with the **bootstrap management-owner authorization key** (owner) and the **bootstrap runtime-signer authorization key** as additional signer (`override_policy_ids: [P_BOOTSTRAP]`); `P_BOOTSTRAP` = ALLOW `signMessage`, DENY `exportPrivateKey`/`exportSeedPhrase`, default-deny rest; signer boundary with structured endorsement input (PLAN §13.1.2, §13.4, D36) · integration: sign endorsement via the boundary, verify with the configured bootstrap key; the bootstrap runtime-signer authorization key **cannot** change policy/signers/owner or export; **no key shared with the assertion wallet** (distinct wallet, management-owner and runtime-signer authorization keys) · TV-G01. · **Status 2026-09-11 (commit d37a6d3):** adapter + unit tests; the bootstrap wallet is created by the provisioning script. Unchecked until provisioned and verified live. **COMPLETE (2026-09-11, commit 399b606):** LIVE: separate Bootstrap Endorsement Key wallet signs an endorsement that `verifyEndorsement` accepts (TV-G01); neither runtime-signer key can sign with the other wallet (D36). Evidence: `artifacts/privy/s001/live-signers.md`.
- [ ] **T5.4 [FAST] — U4 ProvisionAssertionKey + U11 resolver** · deps: T5.2, T3.4 · DID Document projection; `KeyManagementReference`; audit; `GET /v1/dids/{did}` (200/404/410) · tests: application + leak-sentinel crawl · AC-009–012 · TV-C03, I01. · **Status 2026-09-11:** application use case implemented in `trust-anchor-admission.service.ts` (commit 056d5fd) and exercised end to end on Testcontainers PostgreSQL with the labeled FAKE adapters; remaining: `GET /v1/dids/{did}` + leak-sentinel crawl. D39 (2026-09-11): U4 reuses an existing ACTIVE assertion key before creating a wallet; orphan-wallet residual risk accepted.

## Phase 6 — Proof of key possession

- [x] **T6.1 [FAST] — U5 challenge** · deps: T5.4 · CSPRNG nonce, 5-min TTL, one per session. · **COMPLETE (2026-09-11, commit 056d5fd):** 32-byte CSPRNG nonce, 5-minute TTL (domain), one challenge per session (id derived from the session + DB UNIQUE).
- [x] **T6.2 [FAST] — U6a + U6 proof paths** · deps: T6.1, T2.7 · shared verifier; failure → immediate DENY evaluation, no CRE call (D23) · tests TV-D01–D03, D05 · AC-014–018. · **COMPLETE (2026-09-11, commit 056d5fd):** secure-signer and external proof paths share `verifyKeyPossession`; a failed proof is evaluated straight to DENY with no confidential call (application test: wrong key); TV-D01–D03, D05 also covered by the domain tests.
- [x] **T6.3 [FAST] — Atomic consumption** · deps: T6.2, T3.3 · race test on Postgres · TV-D04 · AC-017. · **COMPLETE (2026-09-11, commit e6df1c0):** conditional consume; concurrent race test on PostgreSQL (exactly one wins).

## Phase 7 — Bootstrap Configuration and policy publication

- [ ] **T7.1 [FAST] — Bootstrap config file + tooling** · deps: T5.3, T2.6, T0.8 · `apps/api/config/trust-domains/catenor-one-demo.bootstrap.json` (`acceptedEvidence` from T0.8: `representativeLevelNames: ["id-only"]`; `evidenceProfile: "HYBRID_DEMO"`, `evidenceSources: {company: "SYNTHETIC_MOCK", representative: "REAL_SUMSUB_SANDBOX"}` with MOCK company values until T0.8b (PLAN §20.7.1 H1); **no AML deny-list** (D32); `evidenceMaxAgeDays: 180` [REF-IMPL] on `reviewDate`); `scripts/bootstrap/hash-config.ts` · TV-B01, L04 · AC-004, 077.
- [ ] **T7.2 [FAST] — Integrity checks + public endpoints** · deps: T7.1 · refuse admissions on pin/policy mismatch; `GET /v1/trust-domains/{id}`, `GET /v1/policies/{id}` · TV-B01, B02 · AC-004, 005, 039.
- [ ] **T7.3 [HUMAN][FAST] — Out-of-band acceptance** · deps: T7.1 · maintainer reviews + commits config, sets Railway pin · done-when: commit SHA + pin in BUILD_LOG.

## Phase 8 — `workflows/identity-confidential` (executed by `cre-engineer`)

- [x] **T8.1 [FAST] — Official scaffold → identity-confidential** · deps: T0.7 · `cre init` (`hello-confidential-workflows-ts`; `TERM=dumb` for non-interactive; explicit targets) so `workflows/` is the CRE project root; **`deployment-registry: "private"` written explicitly per target in `workflow.yaml`** (not persisted by `cre init`); `@chainlink/cre-sdk` pin decided and recorded (template pins 1.18.0); `configSchema` uses `z.enum` for `authorizedKeys[].type` so type checks stay on (T0.7); transform to `workflows/identity-confidential` with `main.ts` single HTTP trigger → `handlerInTee` → operation router (`TRUST_ANCHOR_ADMISSION`; unknown → ERROR, zero calls); `secrets.yaml` with the **3** names (`*_VAR`); `shared/safe-log.ts` + lint rule; public configs reviewed for zero secrets; template provenance recorded · TV-E07 · AC-024, 025. No hand-written baseline boilerplate. **COMPLETE (2026-09-11, commit 2f886b1, cre-engineer):** official `hello-confidential-workflows-ts` scaffold (CRE CLI v1.33.0, `@chainlink/cre-sdk` 1.18.0), `workflows/` = CRE project root, private registry per target, 3 secret names, router (unknown operation → ERROR, zero HTTP calls), safe logging, type checks on. Note: `cre init` creates a sub-directory (moved up); simulation needs `--trigger-index 0`.
- [x] **T8.2 [FAST] — Key schedule + sealed context** · deps: T8.1 · sealed-context profile of PLAN §18 (HKDF-SHA256, empty salt, UTF-8 info; AES-256-GCM, 12-byte nonce, `ciphertext ‖ tag`; `aad = UTF-8(operation ‖ runId)`; `notAfter`); **nonce and ciphertext‖tag transported as base64**, decoded with a pure-JS base64 implementation (no `atob` in the runtime) — this base64 path was **not** covered by T0.7 and must pass here in simulation; `fixtures/seal-context.ts`; cross-compat test with Node `crypto` sealing · tests (simulation, production-like limits file): wrong key / other operation / other runId / flipped ciphertext / flipped tag / expired → ERROR. **COMPLETE (2026-09-11, commit 2f886b1):** base64 transport **SIMULATION-CONFIRMED** in the CRE QuickJS/WASM runtime; wrong key / other operation / other runId / flipped ciphertext / flipped tag / expired → ERROR; cross-compat test with Node sealing (`cre-channel.contract.test.ts`). [REF-IMPL] HKDF input = the 32 decoded bytes of the hex channel token; `cb`/`salt` info strings `catenor-one/identity-confidential/{cb,salt}/v1`. Evidence: `artifacts/chainlink/s001/simulation/t8.1-t8.2.md`.
- [ ] **T8.3 [FAST] — Sumsub client + normalizer + `CompanyEvidenceSource` port** · deps: T8.1, T0.8 · pure-JS HMAC signing; REAL call #2 (representative); `CompanyEvidenceSource` port and `NormalizedCompanyEvidence` / `NormalizedRepresentativeEvidence` types; allowlisted normalization with PLAN §20.8 rules N1–N4 (absent labels on completed GREEN = none, otherwise MISSING; `reprocessing` dropped; `reviewDate` only freshness source); error mapping → ERROR · unit tests on the sanitized T0.8 shapes · TV-E04, E05 · AC-021, 022. **Status 2026-09-11 (commit 2f886b1):** signed Sumsub GET of the representative `/one`, allowlisted normalizer (N1–N6), error mapping to ERROR/no facts, unit tests on T0.8 shapes; remaining: explicit `CompanyEvidenceSource` port type, timeout mapping test (TV-E05).
- [x] **T8.3a [FAST] — MOCK company-evidence fixture adapter** · deps: T8.3 · `MockCompanyEvidenceFixture` (source `SYNTHETIC_MOCK`, no HTTP) with the PLAN §20.7 scenarios; scenario only from public config; fixed `reviewDate` literals; binding recorded as `MOCK`; module/name/output labeled MOCK · tests: each scenario's expected facts; source label always `SYNTHETIC_MOCK`; source ≠ Bootstrap Configuration → ERROR · TV-F02–F06, F09, E11, B04 · AC-077. **COMPLETE (2026-09-11, commit 2f886b1):** `mockCompanyEvidence` — all six MOCK scenarios, fixed reviewDate literals, scenario from public config only, labeled MOCK; source ≠ pinned configuration → ERROR `EVIDENCE_SOURCE_MISMATCH` (TV-B04); unit tests per scenario.
- [ ] **T8.3b [BLOCKED — T0.8b] — Real `SumsubCompanyEvidenceSource`** · deps: T0.8b, T8.3 · calls #1 (+#3) behind the same port; replaces the fixture via config + a new Bootstrap Configuration only — no change to `deriveFacts`, policy or domain · tests: same fact-derivation suite passes on recorded real company shapes.
- [ ] **T8.4 [FAST] — Provider-binding gate + fact derivation** · deps: T8.3 · `binding-gate.ts` (externalUserId == bindingRef, types) runs before any derivation; `derive-facts.ts` per PLAN §20.4 as finalized by T0.8; freshness via `runtime.now()` (180 days); null = MISSING · unit tests: happy, AML RED, inactive registry, on hold, stale, missing linkage, non-accepted role, **binding mismatch**, **N1/N2** (GREEN without labels → no labels; non-GREEN absent labels → MISSING), **N3** (`reprocessing` has no effect on facts or commitment), **N4** (freshness from `reviewDate` only), **D32 AML rule** (completed GREEN + no/empty labels → true; RED → false regardless of labels; **completed GREEN + labels present → MISSING with private normalization reason `INCONSISTENT_PROVIDER_STATE`** (N6; recorded in the private run record/trace, absent from public projections; policy then DENY with trace MISSING — TV-F10); pending → false; unparseable → MISSING; a `PEP`- or `SANCTIONS`-labelled input changes only the reason codes, never the rule), **N5** (labels kept as sanitized reason codes, absent from public projections); facts carry evidence-source tags · TV-E01, E02, E11, F02–F06, F09, F10 · AC-040, 042, 043, 075. **Status 2026-09-11 (commit 2f886b1):** binding gate before derivation; §20.4 rules incl. D32 / N6 (`INCONSISTENT_PROVIDER_STATE`), N3, N4, N5; freshness from `runtime.now()`; unit-tested; remaining: per-fact evidence-source tags in the result.
- [ ] **T8.5 [FAST] — Evidence commitment** · deps: T8.4, T2.9 · `commitmentInput` per PLAN §23 (salt via HMAC(HKDF(K), runId); response + provider-ref digests; `evidenceSources`; no `reprocessing`); identical bytes to `test-vectors/s001` goldens · TV-E06, I04 · AC-026, 027, 056, 058. **Status 2026-09-11 (commit 2f886b1):** commitmentInput per §23 with salt = HMAC-SHA256(saltKey, runId) [REF-IMPL: 0x-hex salt and digests]; callback carries it without the salt; the API recomputes it; remaining: the byte-exact golden in `test-vectors/s001` (T2.9).
- [ ] **T8.6 [FAST] — Callback + handler wiring + simulation suite** · deps: T8.2–T8.5, T8.3a · HMAC callback (≤ 10,000-byte body, passed base64-encoded to `HTTPClient`), return `{status, code}` only, preHook (4 calls, 3 secrets, CLOSED); MOCK Sumsub server + MOCK company fixture (both labeled, never confused); run all PLAN §28 cases incl. `tta-mock-company-*` and `tta-source-mismatch` non-interactively with `--limits test/limits.production-like.json`; outputs labeled SIMULATION-CONFIRMED and sanitized · TV-E01, E02, E04–E06, E11, I03 + unknown operation · AC-028, 075. **Status 2026-09-11 (commit 2f886b1):** [REF-IMPL] HMAC-SHA256(cb, timestamp + "." + body) callback; step A (`pnpm test:cre-sim`): S001 through a real `cre workflow simulate` with a MOCK Sumsub server — ALLOW → ACTIVE → TRUST_ANCHOR_VALID and RED → DENY, SIMULATION-CONFIRMED; standalone suite 8/8; remaining: preHook call limit, full PLAN §28 case list, REAL Sumsub sandbox run (step B).
- [ ] **T8.7 [P1][POST-DEMO] — DON-signed result report** · deps: T8.6, first live run (T16.3) · `reportFromDon` over SHA-256 of the result envelope; included in callback; must not block T16.

## Phase 9 — API ↔ CRE integration

- [ ] **T9.1 [FAST] — CRE gateway trigger + context sealer** · deps: T8.6, T4.1 · `workflows.execute` JSON-RPC; EIP-191 JWT (own code, BUSL reference noted in PROVENANCE); AES-256-GCM sealing (Node `crypto`) · unit tests on JWT digest/claims + seal/open golden. **Status 2026-09-11 (commit 2f886b1):** AES-256-GCM context sealer (Node) done; `CreSimulationConfidentialVerifier` drives `cre workflow simulate`; remaining: the deployed-workflow gateway trigger + JWT (gated by B1).
- [ ] **T9.2 [FAST] — U7 RequestConfidentialVerification** · deps: T9.1, T6.2, T4.5 · preconditions: key facts true ∧ refs attached; run + deadline; audit · tests: refuses after failed key proof or without refs · AC-023. · **Status 2026-09-11:** application use case implemented in `trust-anchor-admission.service.ts` (commit 056d5fd) and exercised end to end on Testcontainers PostgreSQL with the labeled FAKE adapters (refuses after a failed key proof or without refs); remaining: the real CRE trigger adapter (T9.1).
- [ ] **T9.3 [FAST] — Callback endpoint + U8** · deps: T9.2 · HMAC/timestamp/single-use; strict schema (unknown fields rejected — TV-F11 intent); echo checks (operation, runId, config hash, freshness, **`evidenceProfile` + `evidenceSources` = Bootstrap Configuration**, TV-B04); **commitment recomputation** against the normalized preimage + digests (store only those; never raw content); OK → facts + `BINDING_VERIFIED`; `PROVIDER_BINDING_MISMATCH`/ERROR → no facts · tests: forged/replayed/late/duplicate/mismatched callbacks rejected; SQL scan for sentinels · TV-E04, E11, I03, I04 · AC-021, 026, 027, 056, 058, 075. · **Status 2026-09-11:** application use case implemented in `trust-anchor-admission.service.ts` (commit 056d5fd) and exercised end to end on Testcontainers PostgreSQL with the labeled FAKE adapters: one result per run, echo checks incl. TV-B04, no facts on ERROR / binding mismatch; remaining: HMAC-authenticated callback endpoint, strict schema, commitment recomputation. Update 2026-09-11 (commit 2f886b1): HMAC + ±5 min timestamp + commitment recomputation + echo checks in `authenticateCallback`; minimal callback receiver; U8 also rejects a result whose execution-mode label differs from the configured verifier.
- [ ] **T9.4 [FAST][POST-DEMO] — U15 run expiry** · deps: T9.2 · deadline sweep → TIMED_OUT → non-ALLOW · TV-E05 · AC-022.
- [ ] **T9.5 [P1][POST-DEMO] — DON report verifier** · deps: T8.7, T9.3 · offchain verification per CRE docs (viem).

## Phase 10 — Policy evaluation

- [ ] **T10.1 [FAST] — Policy/config sources** · deps: T7.2 · integrity re-check at evaluation time.
- [ ] **T10.2 [FAST] — U9 EvaluateAdmission** · deps: T10.1, T9.3, T6.2 · FactSet with provenance; Decision + trace (FALSE vs MISSING); ALLOW → public projection; audit · tests TV-A03, B02, F01–F11 (representative), gate-cannot-set-facts · AC-003, 005, 035–040, 048, 061. · **Status 2026-09-11:** application use case implemented in `trust-anchor-admission.service.ts` (commit 056d5fd) and exercised end to end on Testcontainers PostgreSQL with the labeled FAKE adapters: FactSet with provenance, Decision + private trace (FALSE ≠ MISSING), ALLOW-only projection, audit; remaining: T10.1 integrity re-check source, gate tests (TV-A03). D38 (2026-09-11): early-DENY Decisions commit to `catenor-one/local-decision-evidence/v1` [REF-IMPL]; a Decision after only failed runs is refused (`EVIDENCE_COMMITMENT_UNDEFINED`).

## Phase 11 — Endorsement, Admission Record, activation

- [x] **T11.1 [FAST] — Endorsement builder** · deps: T2.6, T5.3 · payload incl. `decisionCommitment` and `verificationMethodCommitment` (Q1) · AC-044, 076. · **COMPLETE (2026-09-11):** `buildEndorsementPayload` (T2.6) binds `decisionCommitment` and `verificationMethodCommitment`; used by U10 (commit 056d5fd).
- [ ] **T11.2 [FAST] — U10 EndorseAndActivateInitialTrustAnchor** · deps: T11.1, T10.2 · ALLOW-only; human-initiated; sign → verify → record → ACTIVE in one transaction; no mandatory two-person rule · tests TV-B02, B03, G01, G02, G06, H01–H03 · AC-044–051, 076. · **Status 2026-09-11:** application use case implemented in `trust-anchor-admission.service.ts` (commit 056d5fd) and exercised end to end on Testcontainers PostgreSQL with the labeled FAKE adapters: ALLOW-only; sign → verify → record → ACTIVE root in one transaction; remaining: operator-initiated route, application-level TV-B02/B03/G02/G06/H02/H03 tests.
- [ ] **T11.3 [FAST] — Public admission projection** · deps: T11.2 · `GET /v1/trust-domains/{td}/trust-anchors/{did}` · snapshot test · TV-H01 · AC-049.
- [ ] **T11.4 [FAST][POST-DEMO] — Idempotency + retries** · deps: T11.2 · `ALREADY_ADMITTED`; concurrent activation → one root; new session after DENY · TV-K01, K02 · AC-052.

## Phase 12 — Trust Anchor verification

- [ ] **T12.1 [FAST] — U13 VerifyTrustAnchor** · deps: T2.8, T11.3 · endpoint with per-check `basis`; wording per PLAN §26.1. · **Status 2026-09-11:** `verifyTrustAnchor` use case over persisted state returns all 12 checks with their basis (TRUST_ANCHOR_VALID in the application test); remaining: endpoint.
- [ ] **T12.2 [FAST][POST-DEMO] — Tamper/lifecycle integration tests** · deps: T12.1 · DB mutations (evidenceCommitment, policyHash, endorsement, VM key replaced under the same ID) and status via test harness only (D21) · TV-J02, J03, J05, J06, J07 · AC-064, 065, 076.
- [ ] **T12.3 [FAST][POST-DEMO] — Offline verifier script** · deps: T12.1 · `scripts/verify-trust-anchor.ts` (public API + committed config + pin only) · TV-J01, I01 · AC-063.

## Phase 13 — Product UI (`apps/web`)

- [ ] **T13.1 [FAST] — Next.js scaffold + Privy login** · deps: T4.2.
- [ ] **T13.2 [FAST] — Bootstrap wizard** · deps: T13.1, T11.2, T12.1 · steps 1–8 (PLAN §8) incl. provider-setup step showing bindingRefs to the operator; "Sumsub sandbox" wording; server-state only · Playwright E2E with FAKE verifier (labeled) · TV-A01, A02, F01, K02 · AC-066, 069.
- [ ] **T13.3 [FAST] — Wrong-key demo control** · deps: T13.2 · ephemeral browser key; external proof · E2E TV-D02 · AC-015, 067.
- [ ] **T13.4 [FAST] — Public pages** · deps: T13.1, T5.4, T11.3 · `/did/[did]`, `/trust-anchors/[did]` (crypto vs operational checks shown) · AC-009, 063.

## Phase 14 — Judge Inspector

- [ ] **T14.1 [FAST] — Judge projection endpoints** · deps: T10.2, T11.3, T3.4 · allowlisted fields; leak-sentinel crawl incl. bindingRefs · AC-062, 068.
- [ ] **T14.2 [FAST] — `/judge/s001` UI** · deps: T14.1, T13.1 · panels PLAN §9: evidence-profile banner with the exact labels "Company evidence: SYNTHETIC MOCK" and "Representative verification: REAL SUMSUB SANDBOX" (Hybrid Demo Profile, TV-L04, AC-077); facts tagged with their source; representative-authority evidence class + limitation; COMMITMENT_ONLY; "HTTPS requests are executed from inside the confidential TEE boundary"; lifecycle status labeled operational projection · TV-A02, C03, H01, L01–L03 (display) · AC-066–068.
- [ ] **T14.3 [FAST] — Clean static visualization** · deps: — · `artifacts/judges/s001/trust-anchor-flow.html` + README: "visualization only" banner; remove LLM references (PLAN Appendix E.2) · AC-072. · **Status:** banner + Rev 2 wording applied in T0.9; remaining: link to the live `/judge/s001` once T14.2 exists.

## Phase 15 — Railway deployment

- [ ] **T15.1 [FAST] — Services** · deps: T13.2, T3.2 · web + api (root dirs, watch paths, Railpack + pnpm filters), pre-deploy `prisma migrate deploy`, health check; dashboard configuration (D19); staging then production.
- [ ] **T15.2 [FAST][HUMAN + agent] — Infrastructure review** · deps: T15.1 · no Postgres TCP proxy (saved output); internal `DATABASE_URL`; sealed variables; no evidence objects/keys exist (TV-I05 vacuous) · TV-I02, I05 · AC-053.
- [ ] **T15.3 [FAST] — Config pin + env** · deps: T7.3, T15.1 · pin, allowlist, Privy vars; boot check passes · TV-B01.

## Phase 16 — Real CRE deployment and live happy path

- [ ] **T16.1 [HUMAN][FAST] — Vault secrets** · deps: T0.2, T0.4, T8.6 · `cre secrets create workflows/secrets.yaml --target production-settings --secrets-auth=browser` · done-when: `cre secrets list` shows exactly the 3 names (redacted output).
- [ ] **T16.2 [FAST] — Deploy identity-confidential (private registry)** · deps: T16.1, T15.3 · `cre workflow deploy identity-confidential --target production-settings`; record workflow ID, registry (`private`), binary/config URLs, versions; set `CRE_WORKFLOW_ID` on Railway; **first deployed run re-checks the sealed-context open and tamper rejection** before any deployed-TEE claim (T0.7 results are simulation-only) · artifact `artifacts/chainlink/s001/deployment.txt` · AC-029 · TV-E03.
- [ ] **T16.3 [FAST] — Live happy path** · deps: T16.2, T0.8 · evidence profile: `FULL_SUMSUB_SANDBOX` if T0.8b/T8.3b are done, otherwise the **Hybrid Demo Profile** (D31: company SYNTHETIC MOCK, representative REAL Sumsub sandbox, REAL deployed CRE, REAL Catenor policy/endorsement/Admission; H1–H5) · operator → DID + bindingRefs → Sumsub sandbox setup (representative REAL) → attach refs → Privy key proof → deployed TEE (Vault secrets, binding gate, Sumsub sandbox) → ALLOW → endorsement → ACTIVE → verify TRUE (API + offline script) · artifacts `live-happy.json` + judge files · TV-L01, E03, A01, D01, F01, G01, H01, J01 · AC-019, 023, 024, 030, 066, 070.

## Phase 17 — Live negative paths and privacy scans

- [ ] **T17.1 [FAST] — Live invalid-key DENY** · deps: T15.3, T5.4 (can run before T16.3 on a separate session) · wrong-key control → DENY, no endorsement/record/ACTIVE, verification false · TV-L02, D02 · AC-015, 067.
- [ ] **T17.2 [FAST] — Live evidence DENY** · deps: T16.2, T0.8 · REAL Sumsub sandbox representative forced RED (`SANCTIONS`, `FINAL`) → `AUTHORIZED_REPRESENTATIVE_VERIFIED` false → DENY (PLAN §20.6; confirmed feasible in T0.8) · artifact `live-deny-evidence.json` · TV-L03, F04 · AC-036, 067.
- [ ] **T17.3 [FAST] — Privacy/secret scans** · deps: T16.3 · sentinel crawl (applicant IDs, bindingRefs, raw fixtures, emails) of public/judge routes; SQL text scan (staging); secret scanner over repo + `artifacts/` + captured logs (values from env, never committed); private-key pattern scan · TV-E06, E07, I01, I03, I06 · AC-010, 020, 025, 062; ACCEPTANCE §28 list.
- [ ] **T17.4 [P1][POST-DEMO] — Live provider-binding mismatch** · deps: T16.2 · attach an applicant with a wrong `externalUserId` → run ERROR, no facts, non-ALLOW (recorded) · TV-E11 · AC-075.

## Phase 18 — Artifacts, provenance, documentation

- [ ] **T18.1 [FAST] — Assemble artifacts** · deps: T16.3, T17.1, T17.2 · layout PLAN §30; READMEs distinguish MOCK / simulation / live and say "Sumsub sandbox"; REAL vs MOCK evidence claims per PLAN §31.3 (D30, D31): exact labels "Company evidence: SYNTHETIC MOCK" / "Representative verification: REAL SUMSUB SANDBOX", never "Sumsub verified the organization" or "real Sumsub KYB end-to-end"; runs using the company fixture named `*.mock-company.*` · TV-L04 · AC-077 · AC-070–072.
- [ ] **T18.2 [FAST] — Sanitizer** · deps: — (used from T8.6 on) · `scripts/artifacts/sanitize.ts` (applicant IDs, bindingRefs, wallet IDs, emails, tokens) + CI check on `artifacts/`.
- [ ] **T18.3 [FAST] — Integration docs** · deps: T16.3 · `docs/integrations/CHAINLINK-CRE.md` (identity-confidential boundary rule, quotas as documented), `PRIVY.md`, Sumsub section (sandbox) — spike results included.
- [ ] **T18.4 [P1][POST-DEMO] — ADRs + protocol amendment note** · deps: T16.3 · ADR-0007 signer custody; ADR-0008 CRE workflow boundary (`identity-confidential`) + result transport/provenance; ARCHITECTURE §12 / README tree reconciliation (Appendix E.2); Admission Record amendment draft for the protocol repo (D14) · AC-073.
- [ ] **T18.5 [FAST] — Provenance + AI logs** · deps: each phase · BUILD_LOG per phase; AI_USAGE (Claude Code, cre-engineer subagent, official CRE skill, Privy docs skill); PROVENANCE (SDK versions, `cre init` template, BUSL note); prompt artifact for the planning sessions · AC-073, 074.
- [ ] **T18.6 [FAST] — Definition-of-Done audit** · deps: all [FAST] · ACCEPTANCE §29 matrix + §30 DoD; every P0 AC → evidence link; failures reported, not hidden. Includes the Hybrid Demo Profile claims scan over README / artifacts / submission text (TV-L04, AC-077).

---

## Traceability — P0 acceptance criteria → tasks

| AC | Tasks |
|---|---|
| 001–003 | T4.3, T10.2, T13.2 |
| 004–006 | T7.1, T7.2, T2.6, T11.2 |
| 007–008 | T2.2, T4.4 |
| 009–010 | T5.4, T13.4, T17.3 |
| 011–013 | T0.5, T2.2, T5.2 |
| 014, 015, 017 | T2.7, T6.2, T6.3, T13.3, T17.1 |
| 019–021 | T0.8, T0.8b, T4.5, T8.3, T8.3a, T8.3b, T9.3, T16.3, T17.3 — AC-019 (as amended in Rev 2.3) is reported as **met under the Hybrid Demo Profile (company evidence SYNTHETIC MOCK)** until T0.8b enables the Full Sumsub Sandbox Profile; never reported as fully met on MOCK company evidence |
| 023–030 | T0.7, T8.1–T8.6, T16.1–T16.3 |
| 031, 033, 041 | RETIRED (T0.9) — no tasks |
| 075 | T4.4, T4.5, T8.4, T8.6, T9.3, T17.4 |
| 076 | T2.6, T2.8, T11.1, T11.2, T12.2 |
| 077 | T7.1, T8.3a, T9.3, T14.2, T18.1, T18.6 |
| 035–040, 043 | T2.4, T2.5, T8.4, T10.2 |
| 044–051 | T2.6, T11.1–T11.3 |
| 053–056, 058 | T3.1, T3.4, T8.5, T9.3, T15.2 |
| 060–064 | T2.1, T10.2, T12.1–T12.3, T14.1 |
| 066, 067 | T13.2, T16.3, T17.1, T17.2 |
| 070, 072–074 | T18.1, T14.3, T18.5, T0.5–T0.8 |

P1 criteria: 016, 018 (T2.7, T6.2) · 022 (T9.4) · 042 (T8.4) · 052 (T11.4) · 065 (T12.2) · 068, 069 (T14.2, T13.2) · 071 (T18.1). AC-032 and AC-034 RETIRED (T0.9). AC-057 and AC-059 not applicable to S001 (COMMITMENT_ONLY, D13).
