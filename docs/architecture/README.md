# Catenor One Architecture

Decision order:

```text
Catenor Protocol baseline
→ Architecture baseline
→ Slice SPEC
→ Slice PLAN
→ Implementation
```

Canonical files:

```text
ARCHITECTURE.md
CLAUDE-RULES.md
adr/
```

Accepted ADRs should not be silently rewritten during implementation.
