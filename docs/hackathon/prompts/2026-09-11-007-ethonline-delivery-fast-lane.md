# Catenor One — ETHOnline Hackathon Delivery Fast Lane

**Date:** 2026-09-11  
**Project:** Catenor One  
**Scope:** S001 Trust Anchor Admission vertical path, authority-gated Hedera ATS action, Judge Inspector  
**Tool:** Claude Code (main session); `cre-engineer` project subagent for CRE mechanics only  
**Type:** Human prompt to Claude Code — verbatim text of the maintainer's instruction (formatting condensed)  
**Related plan:** `docs/hackathon/plans/2026-09-11-006-ethonline-delivery-fast-lane.md`

## Prompt (verbatim content)

> We are switching Catenor One into HACKATHON DELIVERY MODE. This is a delivery/execution strategy change caused by the ETHOnline deadline. It is NOT an architecture redesign and MUST NOT weaken or rewrite the frozen Catenor/S001 semantics.
>
> **0. DOCUMENT THIS DECISION FIRST.** Before continuing implementation, create a short formal Hackathon Delivery Plan under the existing docs/hackathon/plans naming/date convention. Also create the matching prompt artifact under docs/hackathon/prompts if that is consistent with the current provenance convention. Suggested title: Catenor One — ETHOnline Hackathon Delivery Fast Lane. The document should state: CONTEXT — Deadline is Sunday. Core engineering foundations are already substantially established: Phase 0 research/spikes; framework-free domain packages; RFC8785 / W3C Data Integrity tests; real Sumsub individual sandbox integration; real Privy Ed25519 signing spike; PostgreSQL schema/migration/integration tests. The remaining risk is delivery breadth, integration and demo completion. DECISION — From this point until submission, implementation is optimized for the smallest credible end-to-end demo path. This changes task prioritization, NOT: Catenor Protocol semantics; S001 facts; policy:trust-anchor-admission:v1; Trust Anchor Admission semantics; key-purpose separation; public/private evidence rules; approved evidence profiles; security claims. RULE — TASKS.md remains the engineering backlog and traceability source, but task order is no longer interpreted as "finish every task in one phase before touching the next phase". We may implement only the subset of a task required by the end-to-end demo, provided: the implementation is truthful; unfinished scope remains explicitly unchecked/documented; no fake sponsor integration is represented as real; no frozen semantic is silently weakened.
>
> **1. DEMO DEFINITION OF DONE.** The hackathon MVP is successful when the following vertical story works: A. TRUST ANCHOR ADMISSION — Candidate organization ↓ did:catenor subject ↓ Credential Assertion Key / Privy Ed25519 ↓ proof of key possession ↓ real Sumsub Sandbox representative result + clearly labeled SYNTHETIC MOCK company evidence if KYB entitlement remains absent ↓ Chainlink CRE confidential evaluation (real deployed workflow if enrollment is available; otherwise the strongest truthful simulation path available, explicitly labeled) ↓ normalization ↓ minimal reconciliation ↓ policy:trust-anchor-admission:v1 ↓ ALLOW ↓ Bootstrap Authority endorsement ↓ ACTIVE Trust Anchor. Also demonstrate one DENY path. B. AUTHORITY / ASSET ACTION — The admitted Trust Anchor: ↓ has explicit authority ↓ performs one meaningful downstream RWA action ↓ Hedera ATS testnet is the preferred real execution target. And a subject without the required authority must receive DENY. C. JUDGE VISIBILITY — The Judge Inspector must make the important facts obvious: canonical did:catenor identity; Credential Assertion Key; evidence sources and REAL/MOCK labels; normalized provider result; reconciliation result; policy facts; ALLOW/DENY; bootstrap endorsement; Trust Anchor status; authority/delegation used for the asset action; audit events; sponsor integration evidence. This does NOT require a production-grade general-purpose UI.
>
> **2. AGGRESSIVE SCOPE CONTROL.** Until the hackathon submission is complete, DO NOT build unless required by the demo: generic CRUD; generic repository methods not used by the vertical path; general-purpose APIs; generalized workflow engines; full S002 Subject Continuity implementation; every possible lifecycle transition; exhaustive read-model functionality; sophisticated frontend architecture; production ops that are not needed for the demo; optional P1/P2 hardening; broad refactors; abstractions created only for future use. Do not delete these tasks from TASKS.md. Instead: leave them unchecked; mark them clearly as POST-DEMO / DEFERRED where appropriate; preserve traceability. Rule of thumb: If it is neither required to make the demo execute nor required to make an important technical claim credible, defer it.
>
> **3. CURRENT PERSISTENCE CHECKPOINT.** Before moving to the vertical implementation, fix the already identified AuditEvent append-only gap. Add the approved DB protection: catenor_private.AuditEvent — INSERT allowed; UPDATE denied; DELETE denied. Test it against real PostgreSQL/Testcontainers. Do NOT expand T3.2 beyond this correction. Commit this correction separately. Suggested commit: fix(api): enforce append-only S001 audit events. Update TASKS / BUILD_LOG / PROVENANCE as appropriate.
>
> **4. T3.3 — MINIMUM REPOSITORIES ONLY.** Implement T3.3 only to the extent required by the S001 demo path. Minimum required capabilities: SubjectRegistry (create/find the candidate subject); DidStateRegistry (publish/read DID Document and Verification Method); AdmissionRepository (create/load session; issue/load/conditionally consume challenge; store verification run; store decision/trace; persist Admission Record); TrustAnchorRegistry (establish initial Trust Anchor; read current Trust Anchor status); AuditLog (append event; read demo/audit timeline); UnitOfWork (execute the critical admission transaction atomically). Do NOT add repository methods merely for completeness. Use Prisma only inside infrastructure. Domain packages remain Prisma-free. Test only: behavior required by the demo; concurrency/integrity behavior that protects a critical invariant. Then commit separately. Suggested commit: feat(api): add minimum S001 persistence repositories. Update TASKS / BUILD_LOG / PROVENANCE in the same checkpoint or a small following docs commit according to the existing history style.
>
> **5. AFTER T3.3 — STOP FOLLOWING PHASES SEQUENTIALLY.** Once the minimum persistence adapters work, implement the vertical S001 path across the remaining layers. Prioritize dependencies in this order: 1. application orchestration for one S001 session; 2. Privy assertion signer integration; 3. Bootstrap Endorsement signer; 4. Sumsub real representative adapter; 5. SYNTHETIC MOCK company adapter for Hybrid Demo Profile; 6. normalization + minimal reconciliation; 7. Chainlink CRE workflow/integration; 8. policy execution; 9. Admission persistence / ACTIVE Trust Anchor; 10. one API route/command that runs the flow; 11. one DENY scenario; 12. minimal Judge Inspector. Do NOT stop after every historical phase number unless there is a genuine semantic/security STOP-gate.
>
> **6. RECONCILIATION — MINIMUM DEMO VERSION.** Do not implement a large reconciliation engine. For the demo implement only: VerificationObservation (provider; applicant/provider reference; bindingRef; observedAt; provider status/result; evidence reference/source). Reconciliation result: CONSISTENT; MISMATCH; STALE; INCOMPLETE. Minimum cases: GREEN/current expected state → CONSISTENT → policy may continue; RED where valid/current verification is required → MISMATCH or failed normalized requirement → DENY; stale evidence, if practical → STALE → DENY/refresh required. Do not confuse this with S002 Subject Continuity MATCH/REVIEW/NO_MATCH.
>
> **7. CHAINLINK REALITY RULE.** Chainlink CRE remains important to the demo. Use the existing cre-engineer subagent for CRE mechanics only. If Confidential Workflows deployment enrollment becomes available: → deploy and use the real confidential workflow. If B1 still blocks deployment: → keep the demo path executable using CRE simulation where needed; → label it SIMULATION clearly; → never claim deployed TEE execution. Do not block all application development waiting for B1.
>
> **8. HEDERA FAST LANE.** Do not design a complete RWA platform. Once S001 works, implement the smallest Hedera ATS testnet action that proves the thesis: Catenor authority ↓ policy/authorization check ↓ authorized tokenized asset action on Hedera. Prefer one strong real action over a broad lifecycle. Also demonstrate: unauthorized subject ↓ DENY ↓ no Hedera execution.
>
> **9. FRONTEND / JUDGE INSPECTOR.** Build the minimum UI needed to narrate the architecture. Prioritize: readable; deterministic demo flow; technical evidence; visible sponsor integrations. Do not spend hackathon time polishing generic application screens. One guided demo screen plus Judge Inspector is acceptable.
>
> **10. COMMIT DISCIPLINE — NON-NEGOTIABLE.** Even in Hackathon Delivery Mode, preserve incremental history. Do NOT wait until the entire demo is done to commit. Use small logical commits after stable checkpoints. Expected examples: fix(api): enforce append-only S001 audit events; feat(api): add minimum S001 persistence repositories; feat(s001): orchestrate trust anchor admission flow; feat(privy): integrate S001 assertion and bootstrap signers; feat(sumsub): integrate representative sandbox verification; feat(cre): add confidential S001 verification workflow; feat(s001): complete trust anchor admission vertical slice; feat(hedera): execute authorized RWA demo action; feat(web): add S001 demo and judge inspector; docs(hackathon): record <checkpoint>. Before every commit: run relevant tests; inspect staged files; no .env; no scratch; no secrets; no generated credentials; no amend/squash unless explicitly requested. No push unless explicitly authorized by the maintainer.
>
> **11. DOCUMENTATION DISCIPLINE.** At each meaningful checkpoint update as appropriate: TASKS.md; BUILD_LOG.md; PROVENANCE.md; AI_USAGE.md when the existing convention requires it. Do NOT constantly rewrite SPEC / ACCEPTANCE / TEST-VECTORS. Those remain frozen unless a genuine semantic discrepancy is discovered. For deferred tasks: keep them visible; annotate DEFERRED / POST-DEMO where useful; do not falsely mark incomplete work complete. Add a short "Hackathon Delivery Mode" status note to TASKS/PLAN if needed, but do not rewrite the underlying approved requirements.
>
> **12. TIME BUDGET / DELIVERY PRIORITY.** Optimize for this delivery sequence: THURSDAY → executable S001 vertical path; FRIDAY → authority + Hedera real demo path; SATURDAY → Judge Inspector / frontend / full rehearsal / bug fixing; SUNDAY → video → README/submission material → ETHGlobal dashboard → screenshots → final sponsor claims → submission. Sunday is NOT for new architecture.
>
> **13. EXECUTION NOW.** First: 1. create/update the Hackathon Delivery Fast Lane documentation; 2. apply the AuditEvent append-only correction; 3. test it; 4. commit that correction separately; 5. implement the minimum T3.3 repositories; 6. test; 7. commit that separately. Then continue immediately into the S001 vertical slice instead of completing historical phases exhaustively. STOP only when: a real maintainer action is required; a sponsor credential/enrollment blocks the next executable step; a frozen semantic conflicts with implementation; or a meaningful end-to-end checkpoint is working and needs human review. When stopping, report only: DEMO STATUS: what works end-to-end now; commits created; tests; next demo-critical step; actual blocker, if any; deferred work. We are optimizing for a credible, truthful, working ETHOnline submission by Sunday, not production completeness.
