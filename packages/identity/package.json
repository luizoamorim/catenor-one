{
  "name": "@catenor-one/identity",
  "version": "0.0.0",
  "private": true,
  "description": "Canonical Subject, did:catenor, Verification Methods, Multikey, DID Document, key purposes (framework- and vendor-independent domain package).",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "@catenor-one/source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@scure/base": "2.4.0"
  },
  "devDependencies": {
    "@catenor-one/test-vectors": "workspace:*",
    "@noble/curves": "2.4.0"
  }
}
