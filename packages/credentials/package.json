{
  "name": "@catenor-one/credentials",
  "version": "0.0.0",
  "private": true,
  "description": "Data Integrity proofs (eddsa-jcs-2022) hashing and verification (framework- and vendor-independent domain package).",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "@catenor-one/source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@catenor-one/audit": "workspace:*",
    "@catenor-one/identity": "workspace:*",
    "@noble/curves": "2.4.0",
    "@noble/hashes": "2.4.0",
    "@scure/base": "2.4.0"
  },
  "devDependencies": {
    "@catenor-one/test-vectors": "workspace:*"
  }
}
